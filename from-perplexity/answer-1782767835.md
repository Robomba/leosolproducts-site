# Perplexity (Max) research

**Prompt:** BUILD BRIEF FOR PERPLEXITY (research + frontend build) — Leo Sol Products site / Musicaguide

You are the Perplexity/Comet (research+build) leg of Ro's Command Center. Produce a polished, modern,
static-friendly website frontend. This brief is fully self-contained.

=== CONTEXT (do NOT start from scratch — ELEVATE an existing site) ===
A static landing site already EXISTS and must be extended, not replaced:
- GitHub repo: https://github.com/Robomba/leosolproducts-site  (public, account Robomba)
- Same files are mirrored on the shared drive at: ~/cc-share/projects/leosolproducts-site/
  (= /Volumes/commandcenter/projects/leosolproducts-site/ on the Mac)
- Current files: index.html (the Musicaguide landing page), privacy.html, terms.html,
  vercel.json (uses cleanUrls), README.md.
- It is being deployed on VERCEL to the domain leosolproducts.com (Preset "Other", root "./",
  static — no framework build step). The domain DNS is already on Vercel.

The product the site sells: MUSICAGUIDE — an AI assistant VST3/AU audio plugin for music producers.
Key facts to reflect on the page:
- It is FREE to start and uses BYOK (Bring Your Own Key): the user supplies their own AI API key,
  which is stored locally (macOS Keychain / Windows Credential Manager) — no key is embedded, nothing
  is sent to Leo Sol Products' servers. Privacy-first. Works inside any DAW (Ableton, etc.).
- Available for macOS (universal VST3 + AU + Standalone) and Windows (VST3 + Standalone). Currently
  unsigned (the user may see an OS security prompt on first open — note this gently if useful).
- Download URLs the buttons MUST point to (these are stable "latest release" links):
  - macOS:   https://github.com/Robomba/musicaguide/releases/latest/download/Musicaguide-macOS.zip
  - Windows: https://github.com/Robomba/musicaguide/releases/latest/download/Musicaguide-Windows.zip

=== GOAL (two deliverables) ===
1) A STRONGER Musicaguide product page (replacing / upgrading the current index.html):
   - Clean modern hero (product name, one-line value prop, primary download CTAs).
   - Separate, clearly labeled download buttons for macOS and Windows wired to the URLs above.
   - Feature highlights section (e.g. "AI audio engineer inside your DAW", BYOK/privacy, works in any
     DAW, free to start, markdown-rich answers).
   - A short BYOK explainer section (what BYOK means, where the key is stored, why it's private).
   - Footer with links to privacy.html and terms.html (keep those pages working; restyle them to match).
   - Tasteful, fast, accessible, responsive. No heavy frameworks required.

2) A REUSABLE FRONTEND PATTERN / TEMPLATE so every FUTURE shipped product gets its own page under the
   SAME site with minimal effort:
   - Factor shared styling into one stylesheet (e.g. /assets/styles.css) and shared structure (header,
     footer, hero, feature grid, download-CTA block) into reusable, copy-pasteable components/partials.
   - Provide a documented "product page template" (e.g. /products/_template.html or a clearly commented
     boilerplate) plus a short README section explaining: "to add a new product, copy the template,
     fill in name / tagline / features / download links, add a card to the index."
   - Make the Musicaguide page itself an INSTANCE of that template so the pattern is proven.
   - Optionally turn the top-level index into a simple multi-product landing (a card grid of products)
     with Musicaguide as the first card, while keeping a dedicated Musicaguide page — your call, but
     keep it simple and static.

=== HARD CONSTRAINTS (compatibility — do not break the deploy) ===
- 100% STATIC. Plain HTML/CSS (+ tiny vanilla JS only if needed). NO build step, NO bundler, NO
  framework that requires compilation — it must deploy as-is on Vercel "Other"/root "./".
- Keep it compatible with the existing vercel.json cleanUrls behavior (so /privacy and /terms style
  links keep working). If you add routes, keep relative paths clean.
- Keep privacy.html and terms.html present and functional (restyling them to match is welcome).
- Keep all existing download links pointing to the GitHub Releases URLs above (do not change targets).
- Fast and lightweight: inline or local assets, no large dependencies, good Lighthouse-ish hygiene.

=== OUTPUT (what to hand back) ===
- Deliver the actual files (index.html / product pages / assets/styles.css / template file / updated
  privacy.html + terms.html + README notes). Write them so they can be dropped straight into
  ~/cc-share/projects/leosolproducts-site/ and committed to the repo with no edits.
- Include a short "what changed + how to add the next product" note at the top of your output so Ro
  can review quickly.
- Do NOT push to GitHub, deploy to Vercel, or sign into anything — just produce the files/output.
  Shipping (commit + Vercel deploy) is gated to Ro.


Here is a drop-in upgrade for your existing repo that elevates the current site and establishes a reusable product pattern.
What changed (quick)
Introduced a shared design system in /assets/styles.css (modern, responsive, lightweight).
Upgraded Musicaguide into a proper product page (now /products/musicaguide.html).
Converted index.html into a simple multi-product landing (card grid).
Added a reusable product template (/products/_template.html).
Restyled privacy.html and terms.html to match.
